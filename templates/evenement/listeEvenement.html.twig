{% extends 'base.html.twig' %}

{% block title %}Liste des Événements{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Liste des Événements</h1>
            <a href="{{ path('evenement_ajouter') }}" class="btn btn-primary">
                <i class="fas fa-plus"></i> Nouvel Événement
            </a>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        <div class="row">
            {% for evenement in evenements %}
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <img src="{{ image_base_url ~ evenement.photoEvent }}" class="card-img-top" alt="{{ evenement.nomEvent }}"
                        <div class="card-body">
                            <h5 class="card-title">{{ evenement.nomEvent }}</h5>
                            <p class="card-text">
                                <i class="fas fa-calendar"></i> {{ evenement.dateEvent ? evenement.dateEvent|date('d/m/Y H:i') : '' }}
                            </p>
                            <p class="card-text">
                                <i class="fas fa-map-marker-alt"></i> {{ evenement.lieuEvent }}
                            </p>
                            <p class="card-text">
                                <i class="fas fa-layer-group"></i>Catégorie : {{ evenement.categorie ? evenement.categorie.nom : 'Non spécifiée' }}
                            </p>
                            <p class="card-text">
                                <i class="fas fa-users"></i> Places restantes : {{ evenement.maxPlacesEvent }}
                            </p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a href="{{ path('evenement_detailles', {id: evenement.id}) }}" class="btn btn-sm btn-outline-primary">
                                        <i class="fas fa-eye"></i> Voir
                                    </a>
                                    <a href="{{ path('evenement_modifier', {id: evenement.id}) }}" class="btn btn-sm btn-outline-secondary">
                                        <i class="fas fa-edit"></i> Modifier
                                    </a>
                                </div>  
                                    <input type="hidden">
                                    <a href="{{ path('evenement_supprimer', {id: evenement.id}) }}" 
                                        onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet événement ?');" 
                                        class="btn btn-sm btn-outline-danger">
                                        <i class="fas fa-trash"></i> Supprimer
                                    </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12">
                    <div class="alert alert-info">
                        Aucun événement n'a été trouvé.
                        <a href="" class="btn btn-primary btn-sm ml-3">
                            <i class="fas fa-plus"></i> Créer un événement
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
